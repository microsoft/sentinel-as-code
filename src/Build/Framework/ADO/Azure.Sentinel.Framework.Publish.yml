# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger: none

parameters:
  - name: AzureDevOpsProject
    displayName: Azure DevOps Project
    type: string
    default: 'Microsoft Projects'

pool:
  vmImage: 'windows-latest'

resources:
  pipelines:
    - pipeline: build_1
      source: "Sentinel Artifacts Framework (Build)"

steps:
